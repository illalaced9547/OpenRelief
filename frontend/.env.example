# Base URL of the live inference endpoint (src/open_relief/serve.py), e.g.
# https://<job>.<region>.nebius.cloud - no trailing slash.
# When unset, the chat always falls back to the synthetic demo answers.
VITE_INFERENCE_URL=
